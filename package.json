{
  "name": "sustainplus-site",
  "version": "1.0.0",
  "private": true,
  "description": "SustainPlus environmental consulting website — pure HTML/CSS/JS with Sanity-backed project data",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve .",
    "fetch-content": "node build.js"
  },
  "engines": {
    "node": ">=18"
  }
}
